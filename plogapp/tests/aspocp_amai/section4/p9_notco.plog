sorts
#boolean = {true, false}.

attributes
a: #boolean.
b: #boolean.
c: #boolean.
d: #boolean.
f: #boolean.
e: #boolean.

statements

f.
random(a) :- not b, f.
pr(a) = 3/10.
c :- a.
d :- a = false.

e :- a. 
e :- not e.


% the program produces an error, but not a readable one.
%?c.
?d.
