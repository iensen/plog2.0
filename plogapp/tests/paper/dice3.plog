sorts

#bool = {true, false}.
#dice={d1,d2}.
#score={1,2,3,4,5,6}.
#person={mike,john}.


attributes

roll:#dice->#score.
owns:#dice,#person->#bool.

statements

owns(d1,mike).
owns(d2,john).

random(roll(D)).

%probability information

pr(roll(D)=6|owns(D,mike))=1/4.

? roll(d2)=6.
